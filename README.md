# vanilla-flat-white

Microprojects in Vanilla JavaScript.

See the live site [here](https://vandivier.github.io/vanilla-flat-white)!

# deploying a new version

Make sure that you only run `npm run deploy`:

1. from the `main` branch
2. while there are no active changes
3. when main and origin/main are even
